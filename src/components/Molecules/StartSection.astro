---
export interface Props {
  title: string;
  description?: string;
  href?: string[];
}

const { title, description, href } = Astro.props;
---

<h1>{title}</h1>
<p>
  {description}
</p>
{href?.map((href) => <a href="">{href}</a>)}
